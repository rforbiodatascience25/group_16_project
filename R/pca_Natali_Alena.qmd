---
title: "pca_Natali_Alena"
format: html
---

# PCA Plot

### Load Libraries

```{r}
library(tidyverse)
library(ggbiplot)
library(devtools)

```

### Transpose

```{r}
transp_expr <- t(expr_data)
```

```{r}
transp_expr <- data.frame(transp_expr)
```
